Orchest mail templates generator
===========

## Installing requirements

`npm install`

## Building mail templates

`npm run build`

## Auto build on file change

`npm run watch_build`

The build results are in the `/build` directory.
